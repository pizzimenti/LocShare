{
  "rules": {
    ".read": false,
    ".write": false,
    "shares": {
      "$token": {
        ".read": "data.child('expiresAt').val() === 0 || data.child('expiresAt').val() > now",
        ".write": "auth != null && ((!data.exists() && newData.exists() && newData.child('owner').val() === auth.uid) || (data.exists() && data.child('owner').val() === auth.uid) || (!data.exists() && !newData.exists()))"
      }
    }
  }
}
